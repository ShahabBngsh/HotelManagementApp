<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CustomerDetailsFragment"
    android:background="@color/black">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:id="@+id/customer_details_search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@drawable/top_bar_edges"
        >

        <SearchView
            android:id="@+id/home_search"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginHorizontal="20dp"
            android:closeIcon="@drawable/ic_baseline_close_24"
            android:searchIcon="@drawable/ic_baseline_search_white"
            android:iconifiedByDefault="true">

            <requestFocus />
        </SearchView>



    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="8"
        android:layout_below="@+id/customer_details_search_bar"
        >

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/customerDetailRV"
            android:layout_marginTop="20dp"
            android:background="@color/black"

            />
    </LinearLayout>

</RelativeLayout>